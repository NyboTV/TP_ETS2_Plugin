## Known Issues

- Switching between currencies doesn't work properly.
The Plugin starts to flicker between the values.

        I Changed it so the Income is now directly outputted as a string, which should fix the issue in the meantime.
        If i got a new idea i will try it out.